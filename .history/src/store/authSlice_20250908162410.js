import { createSlice } from "@reduxjs/toolkit"


const initialState = {
    status: true,
    userData: null,
}

createSlice({
    name: "blog",
    initialState,
    reducers:{
        login: (state, )
    }
})